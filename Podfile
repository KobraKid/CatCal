platform :ios, '8.0'
use_frameworks!
target 'CatCal' do
    # Google API
    pod 'GoogleAPIClientForREST/Calendar', '~> 1.2.1'
    pod 'Google/SignIn', '~> 3.0.3'
    # SwiftyJSON for simple, clean JSON parsing:
    pod 'SwiftyJSON'
    # SwiftyBeaver for colorful logging:
    pod 'SwiftyBeaver'
end
